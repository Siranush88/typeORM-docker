{
  "type": "module",
  "name": "docker-typeorm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --require dotenv/config ./build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && npx tsc",
    "start": "npm run build && npm run watch && node build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "pg": "^8.11.1",
    "pg-promise": "^11.5.0",
    "rimraf": "^5.0.1",
    "sql.js": "^1.8.0",
    "typeorm": "^0.3.17",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2"
  }
}
